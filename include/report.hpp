#ifndef REPORT_HPP
    #define REPORT_HPP

    #include "required.hpp"

    struct report
    {
        std::vector<std::string> data;
        std::vector<std::size_t> line;
    };

#endif /* REPORT_HPP */
